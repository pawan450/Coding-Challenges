from typing import List

class Solution:
    def maxProfit(self, prices: List[int]) -> int:

        l = 0

        charSte = set()

        for r in range(len(s)):

            while s[r] in set:

                charset.remove(s[l])
                l +=1

            charset.adds[r]

            res = max(res, r -l + 1)    

        return  res







charSet = {}
